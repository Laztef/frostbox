name: frostbox
description: "This is a simple widget to get a frosted container."
version: 0.0.3
repository: https://github.com/Laztef/frostbox

environment:
  sdk: ^3.10.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

  assets:
    - assets/
  
